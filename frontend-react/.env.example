# Vite environment variables for React frontend

# API Base URL (relative path - proxies to backend)
# In development: uses Vite proxy to http://localhost:8000
# In production: uses relative path /api
VITE_API_BASE_URL=/api
